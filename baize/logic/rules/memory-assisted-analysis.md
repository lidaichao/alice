# 记忆辅助分析规则

- Claude Code 每次处理服务器请求时，必须先判断当前请求是否需要记忆辅助分析。
- 如果请求涉及历史决策、项目背景、用户偏好、长期规则、既往 Bug、模块归属、人员分工、Jira 习惯或跨会话上下文，应主动使用浅层记忆和可用上下文辅助判断。
- 如果当前请求只涉及一次性事实、明确文件内容或无需历史背景的代码定位，可以不依赖记忆，但不得忽略已经提供的相关记忆。
- 如果提供的记忆不足以支持结论，应在回复中说明缺口，并优先基于代码、附件、Jira 快照和已确认逻辑规则继续分析。
