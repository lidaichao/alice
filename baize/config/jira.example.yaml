enabled: false
# 自建 Jira Server/Data Center 示例，地址可以是局域网 IP 或内网域名。
baseURL: "http://192.168.10.10:8080"
deploymentType: server
apiVersion: "2"
authType: basic
username: "jira-user"
password: ""
# 如果你的自建 Jira 使用 Personal Access Token，可以改成：
# authType: bearer
# apiToken: ""
defaults:
  projectKey: ""
  # projectKey: "AL"（按团队实际 Jira 项目填写）
  issueType: "Task"
fields:
  storyPoints: "customfield_10016"
  taskOwner: "customfield_10130"
