---
description: 杰尼龟标准操作索引——测试矩阵 + 技能导航（≤20行）
alwaysApply: true
---

# 杰尼龟操作索引

## 关联测试

| 改动范围 | 运行 |
|----------|------|
| `backend/ai_bridge.py` / `cursor_agent_lane.py` | `pytest backend/tests/ -v` |
| `agent_graph.py` / v3.0 新增 | `pytest backend/tests/test_agent_graph.py` |
| `frontend/src/` | `npm run build` |
| `admin-ui/src/` | `npm run build` |

Windows：`$env:PYTHONIOENCODING='utf-8'` 后再跑 pytest。

## 服务器部署（2026-06-13 火星计划后生效）

- **Alice 生产地址**：`http://192.168.72.147:5000`
- **部署流程**：`ssh alice@192.168.72.147` → `cd alice` → `git pull` → `docker compose -f docker-compose.prod.yml up -d --build`
- **验证约束**：部署后禁止用 `localhost` 路径验证功能——必须以服务器地址打开浏览器/curl 确认
- **战报要求**：含 `curl http://192.168.72.147:5000/health` 输出（替代本地 health check）
- **核心地址**：Chat `:5000/chat/` · Admin `:8080` · n8n `:5678` · Dify Web `:5001`

## 自动加载的 Skills

| 场景 | Skill |
|------|-------|
| 新会话 / 兔子变更通知 | `.cursor/skills/squirtle-startup-check/SKILL.md` |
| 开发令（含子任务） | `.cursor/skills/squirtle-pre-dev/SKILL.md` |
| Jira 操作 | `.cursor/skills/squirtle-jira-ops/SKILL.md` |
| 战报输出 | `.cursor/skills/squirtle-battle-report/SKILL.md` |
| TEAM_CHANNEL 留言 | `.cursor/skills/squirtle-team-channel/SKILL.md` |
| 提交前 v3.0 守卫 | `.cursor/skills/squirtle-v3-guard/SKILL.md` |
| 蓝图/架构规划 | `.cursor/skills/squirtle-roadmap/SKILL.md` |
