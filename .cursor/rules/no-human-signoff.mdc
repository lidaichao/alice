---
description: 近期开发不以 MD 签字/人工验收纪要作为完成条件
alwaysApply: true
---

# 无人工签字验收

团队约定（产品负责人确认）：

- **不要求**在 `eval/reports/*.md`、里程碑纪要、PRD checklist 上人工「签字」或勾选灰盒场景。
- 里程碑/发版 **完成条件** = WBS `[x]` + `scripts/ci_gate.py` → `CI_GATE_OK`；若 Hub 在线则再加 `ALICE_RUN_INTEGRATION=1` 跑 smoke/e2e。
- 可保留 `eval/reports/release_YYYY-MM-DD.md` 仅作 **自动化命令输出存档**，不写签字栏、不阻塞合并。
