---
description: 任务完成后自动 git commit 并 push 到 GitHub，无需用户再次指示
alwaysApply: true
---

# 自动提交 GitHub（常驻策略）

完成实现类任务后，**自行**执行提交并推送到 GitHub，不要等用户说「提交一下」。

## 必须做

1. 任务收尾时检查 `git status`；有应提交的改动则 **commit + push**
2. 遵循仓库既有 commit 风格；message 聚焦 **why**
3. 汇报中写明：**commit hash**、分支、是否 push 成功

## 不要做

- 不要提交密钥、`global_config.json`、`backend/runtime/`、无关研究记忆
- 不要 `git push --force`、不要改 `git config`
- 用户当轮明确禁止提交时，以当轮指令为准

## 细则

完整步骤见项目 Skill：`.cursor/skills/auto-commit-github/SKILL.md`
