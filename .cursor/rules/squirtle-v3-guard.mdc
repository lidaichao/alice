---
description: v3.0 执行纪律守卫——6 轮全栈评估的 15 条硬约束落地（2026-06-10 猫头鹰+兔子联合制定）
alwaysApply: true
---
# 杰尼龟 v3.0 执行守卫

> 本文件是 `ALICE_V3_RESTRUCTURE_PLAN.md` §八 22 条约束的**代码落地版**。
> 每一条背后都有一个"如果不遵守 → Alice 崩"的翻车场景。
> 每次提交前自检本清单。缺任一条 → 战报判黄。

## 1. Mock 挡板数据保真

**Mock n8n 返回的 JSON 必须是真实 Jira API 响应体的完整副本**——不得简写、不得手写扁平 `{"status":"ok"}`。

- 用 curl/Postman 抓取一次真实 `POST /rest/api/2/issue` 成功响应
- 原封不动硬编码到 `send_to_n8n_mock()` 的返回值
- **不遵守 → Phase 4 通过但 Phase 2 切真实 n8n 后前端 `TypeError` 白屏**

## 2. Mock 开关 = 环境变量

```python
MOCK_N8N = os.getenv("ALICE_MOCK_N8N", "False").lower() in ("true", "1", "yes")
```

禁止硬编码 `MOCK_MODE = True`。不遵守 → 忘记切 False 提交 → 生产环境全假执行。

## 3. Pydantic 出入参强校验

`ai_bridge.py` 路由入口和 HITL 桥接器必须用 `BaseModel` 校验 JSON payload：

```python
from pydantic import BaseModel, Field

class ConfirmCardPayload(BaseModel):
    idempotency_key: str = Field(..., pattern=r'^alice-tx-[a-f0-9\-]+$')
    action: str = Field(..., pattern=r'^(create_issue|update_issue|add_comment)$')
```

不遵守 → LLM 幻觉 JSON → 非标字段穿透 → 后端数据污染。

## 4. 幂等 Key = Jira Label（不是 n8n StaticData）

```python
import re, uuid
raw_id = str(uuid.uuid4())
idempotency_key = f"alice-tx-{re.sub(r'[^a-zA-Z0-9\-]', '', raw_id)}"
```

Key 格式必须正则清洗（去特殊字符），透传给 n8n → Jira Label `alice-tx-{key}` 去重。
不遵守 → Jira API 400 或重复创建脏数据。

## 5. LLM 调用必须 Hard Timeout 60s

`agent_graph.py` LLM 节点：

```python
import concurrent.futures
with concurrent.futures.ThreadPoolExecutor(max_workers=1) as executor:
    future = executor.submit(llm.invoke, state["messages"])
    return future.result(timeout=60)
```

不遵守 → 1 个慢 LLM 占住 Waitress Worker → 僵尸线程堆积 → 全服务假死。

## 6. SSE 连接 = 前后端双重生命周期

前端 (`ChatView.tsx`)：

```tsx
useEffect(() => {
  const controller = new AbortController();
  fetchSSE(url, { signal: controller.signal });
  return () => controller.abort();  // 组件卸载 → 断开连接
}, [sessionId]);
```

后端 (`ai_bridge.py`)：

```python
def sse_event_stream():
    try:
        for event in agent.stream(input):
            yield f"data: {json.dumps(event)}\n\n"
    except GeneratorExit:  # 客户端断开 → 归还 Worker 线程
        pass
```

不遵守 → 用户刷新页面 → 残留僵尸连接 → 20 个线程几分钟耗尽。

## 7. 前端请求互斥锁

Chat 输入区：当前有 SSE Agent 流在处理中时，物理禁用输入框和发送按钮。

```tsx
<button disabled={isAgentBusy || !input.trim()}>
  {isAgentBusy ? "AI 思考中..." : "发送"}
</button>
```

Alice 已有 `generatingSessions` Zustand state，仅需绑定 `disabled` 属性。
不遵守 → 用户狂点 → Waitress accept 队列溢出。

## 8. 全局日志 = loguru + TraceID

```python
from loguru import logger
import uuid
trace_id = str(uuid.uuid4())[:8]
logger.info(f"[{trace_id}] LangGraph Agent 开始推理")
```

级别纪律：

| 级别 | 生产环境 |
|------|----------|
| DEBUG（含 LangGraph State 全量 Payload） | ❌ 禁止 |
| INFO（请求入口/耗时/状态码） | ✅ |
| ERROR（异常堆栈） | ✅ |

不遵守 → DEBUG 级 LangGraph State 大段 RAG 上下文 → C 盘几天撑爆。

## 9. 日志滚动：50MB / 5天 / zip

```python
logger.add("logs/alice_{time:YYYY-MM-DD}.log",
           rotation="50 MB", retention="5 days", compression="zip",
           level="INFO")
```

不遵守 → 同上，磁盘耗尽。

## 10. 死代码 Import 零残留

CI 扫描禁止以下 import 出现在 `backend/` 任何 `.py` 文件中：

```
import faiss
from jira_api import
knowledge_retriever
rag_engine
react_runner
workflow_engine
```

不遵守 → 旧引擎幽灵代码回流 → 重构不彻底。

## 11. n8n 配置 = Git 唯一真源

- n8n Docker 容器视为无状态、可随时销毁
- 任何在 n8n Web UI 拖拽修改的节点逻辑 → 必须导出 JSON 覆盖代码库 → commit
- 未纳入 Git 追踪的 n8n 配置 = 无效交付
- Phase 5 验收：重建容器 + 导入 Git JSON → 回归测试通过

不遵守 → 换环境部署时工作流完全对不上 → 全员卡死。

## 12. Phase-by-Phase 渐进交付

禁止"大爆炸"全量合并（一个 MR 包含 Phase 1-5 全部代码）。

- 每个 Phase 单独 MR
- 每个 MR 携带该 Phase 的 pytest 全绿 + `npm run build` 通过
- CI 自动触发 ruff/flake8 检查

## 13. 依赖版本 = 精确 Patch

`requirements.txt` 和 `package.json` 中禁止 `>=` 或 `~=` 范围约束。
不遵守 → `pip install` 自动拉取上游 Breaking Change → 大面积报错。

## 14. 生产 Fail-fast Mock 检查

Hub 启动入口：

```python
if os.getenv("FLASK_ENV") == "production" and \
   os.getenv("ALICE_MOCK_N8N", "").lower() in ("true", "1", "yes"):
    sys.exit("❌ 致命错误：生产环境禁止启用 ALICE_MOCK_N8N。")
```

不遵守 → Mock 挡板误入生产 → 全站假执行 → 线上事故。

## 15. 每次提交前自检

| # | 检查项 | 约束来源 |
|---|--------|----------|
| ✅ | Mock 返回 = 真实 Jira API 响应完整副本 | §2.5.10 / 约束#14 |
| ✅ | MOCK_N8N 走环境变量，未硬编码 | §2.5.7 / 约束#18 |
| ✅ | 路由入口有 Pydantic BaseModel 校验 | §2.5.16 / 约束#20 |
| ✅ | idempotency_key 正则清洗过 `[^a-zA-Z0-9\-]` | §2.5.8 / 约束#10 |
| ✅ | LLM 调用有 `future.result(timeout=60)` | §2.5.6 / 约束#11 |
| ✅ | 前端 useEffect cleanup 有 `AbortController.abort()` | §2.5.11 / 约束#15 |
| ✅ | 后端 SSE Generator 捕获了 `GeneratorExit` | §2.5.11 / 约束#15 |
| ✅ | Chat 输入框 `isAgentBusy → disabled` | §2.5.9 / 约束#12 |
| ✅ | 日志用 `loguru` + `TraceID` + `INFO` 级别 + 50MB 滚动 | §2.5.12/14 / 约束#16-17 |
| ✅ | 无 `import faiss` / `jira_api` / `react_runner` 等废弃模块 | §2.5.17 / 约束#21 |
| ✅ | n8n 工作流 JSON 已导出覆盖 Git | §2.5.18 / 约束#22 |
