# 爱丽丝 AI Gateway · Alice v3.0
# 安装: pip install -r requirements.txt
# 约束#13：所有依赖锁定精确 Patch 版本，禁止 >= 或 ~=

# Web 框架
flask==3.1.1
flask-cors==6.0.1
waitress==3.0.2

# HTTP + 异步
requests==2.32.4
httpx==0.28.1
aiohttp==3.14.1

# 重试
tenacity==9.1.2

# MCP 服务（可选，供 WorkBuddy 调用）
fastmcp==2.12.5

# Cursor SDK（P2-2 Cursor Lane 通用执行引擎）
# ⚠️ v1.0.30 已知兼容性问题：cursor-sdk 0.1.7 在 Python 3.14 + Windows 上
# Agent.create() 因 selectors.SelectSelector + select.select() 不兼容 pipe 句柄
# 而报 OSError WinError 10038。客户端本地执行不受影响（客户端可能有其他 Python 版本）。
# 锁定版本为 0.1.7，待上游修复后升级。
cursor-sdk==0.1.7

# 环境变量
python-dotenv==1.1.0

# 生产级 WSGI（Docker 中替代 Waitress）
gunicorn==23.0.0

# -- v3.0 Phase 0-1 新增（约束#13：精确 Patch 锁定）--

# LangGraph Agent 编排
langgraph==1.2.4
langchain-core==1.4.3
langchain-openai==1.3.0
langgraph-checkpoint==4.1.1
langgraph-checkpoint-sqlite==3.1.0

# 日志（约束#16-17）
loguru==0.7.3

# API 契约校验（Phase 0.11 · 约束#20）
pydantic==2.12.0
