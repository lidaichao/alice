# 爱丽丝 AI Gateway
# 安装: pip install -r requirements.txt

# Web 框架
flask>=3.1
flask-cors>=4.0
waitress>=3.0

# HTTP + 异步
requests>=2.32
aiohttp>=3.10

# 重试
tenacity>=8.0

# MCP 服务（可选，供 WorkBuddy 调用）
fastmcp>=2.0

# 环境变量
python-dotenv>=1.0

# 生产级 WSGI（Docker 中替代 Waitress）
gunicorn>=22.0
