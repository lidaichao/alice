# AliceV2 / Baize local hub — copy to `.env` and fill in values (never commit `.env`).

# Cursor SDK (P2.5 production chat provider)
CURSOR_SDK_KEY=
CURSOR_SDK_MODEL=composer-2.5
# CURSOR_SDK_WORKSPACE=H:/workbuddy/aliceV2/baize

# Optional: override chat provider without editing baize/config/claude.yaml
# BAIZE_CHAT_PROVIDER=cursor

# Anthropic (engineering / Claude Code only — not production chat in coordinator region)
# ANTHROPIC_API_KEY=
# ANTHROPIC_AUTH_TOKEN=
